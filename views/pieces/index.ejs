<h2 class="text-center">Pieces List</h2>

<!-- DELETE CODE BELOW THIS LINE -->


<% if (pieces && pieces.length > 0) {%>
	<% pieces.forEach(p => {%>

	<div>
		<a href="/pieces/<%=p.id%>"> <%=p.name%></a>
		(<%=p.museum.name%> — <%=p.museum.city%>)
	</div>

	<%});%>
	
<%} else {%>
	<h4> No pieces yet </h4>
	<p> Try adding a new piece on the <a href="/pieces/new"> piece registration page </a></p>
<%}%>


