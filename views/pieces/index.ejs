<h2 class="text-center">Pieces List</h2>

<% pieces.forEach(p => { %>
	<p>
		<a href="/pieces/<%=p.id%>">
			<%= p.name %> by <%= p.creator.firstname %> <%= p.creator.lastname %>
		</a>
		(<%= p.originCountry %>)
	</p>
<% }) %>