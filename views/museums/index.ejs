<h2 class="text-center">Museums List</h2>

<div>
    <% museum.forEach(m => { %>
        <div>
            <p><%=m.city%> <%=m.country%></p>
            <a href='/museums/<%=m._id%>'><%=m.name%></a>
            <img src="<%=m.image%>">
        </div>
    <% }) %>
</div>